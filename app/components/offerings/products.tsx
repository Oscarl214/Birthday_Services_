import React from 'react';

const Products = () => {
  return (
    <div>
      Make this a card and proceed to loop over with from the data fetch from
      the database, need to upload images of products to s3 bucket and add to
      according product, then map over the products on to one card that repeats,
      from there figure out how to make another page to which when user clicks a
      product it will show the full product by itself with more details and
      shows the how to book.
    </div>
  );
};

export default Products;
